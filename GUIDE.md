# 📖 Гайд: Как правильно писать Telegram ботов

Добро пожаловать! Этот гайд объяснит архитектуру и лучшие практики разработки ботов на примере нашего проекта.

---

## 🏗️ Принцип 1: Разделение по ответственности (Separation of Concerns)

**Идея:** Каждый файл отвечает за ОДНУ область функциональности.

### Почему это важно?
- **Легче искать ошибки** — если что-то сломалось с базой, ищешь в одном файле
- **Легче тестировать** — можешь тестировать части отдельно
- **Легче развивать** — добавляешь новые фичи без мешанины
- **Легче читать** — другие разработчики поймут структуру

### Структура нашего проекта:

```
├── config.py          ← только конфигурация и переменные окружения
├── models.py          ← только модели БД (структуры данных)
├── database.py        ← только работа с БД (CRUD операции)
├── keyboards.py       ← только UI элементы (кнопки, маппинги)
├── handlers/
│   ├── commands.py    ← обработчики команд пользователя
│   └── callbacks.py   ← обработчики нажатия на кнопки
├── notifications.py   ← только рассылка уведомлений
├── ai_client.py       ← интеграция с внешним ИИ
└── bot.py             ← сборка всего вместе (главный файл)
```

---

## 📁 Разберем каждый модуль

### 1️⃣ `config.py` — Конфигурация

```python
# Что здесь:
BOT_TOKEN = os.getenv("BOT_TOKEN")
DB_URL = os.getenv("DATABASE_URL")
```

**Правило:** Все переменные окружения и конфиги в одном месте!

**Почему?**
- Не нужно искать `.getenv()` по всему коду
- При смене сервера меняешь только этот файл
- Безопасность: токены не разбросаны по коду

**❌ НЕПРАВИЛЬНО:**
```python
# handlers/commands.py
BOT_TOKEN = os.getenv("BOT_TOKEN")  # ← плохо, конфиг в обработчике
```

**✅ ПРАВИЛЬНО:**
```python
# config.py
BOT_TOKEN = os.getenv("BOT_TOKEN")

# handlers/commands.py
from config import BOT_TOKEN  # ← импортируем из конфига
```

---

### 2️⃣ `models.py` — Модели БД

```python
class Task(Base):
    id = Column(Integer, primary_key=True)
    user_id = Column(Integer, nullable=False)
    description = Column(String, nullable=False)
    # ...
```

**Правило:** Описание структуры данных в одном месте!

**Почему?**
- Если нужно добавить поле (например, приоритет), меняешь в одном месте
- Базовая информация о том, как хранятся данные
- Помогает понять логику приложения

**❌ НЕПРАВИЛЬНО:**
```python
# database.py
from sqlalchemy import Column, String, Integer
class Task(Base):  # ← модель разбросана по разным файлам
    ...
```

**✅ ПРАВИЛЬНО:**
```python
# models.py — только модели
class Task(Base):
    ...

# database.py
from models import Task  # ← импортируем готовую модель
```

---

### 3️⃣ `database.py` — Работа с БД (CRUD)

```python
def save_task(task: Task) -> Task:
    """Сохранить задачу в БД"""
    s = get_session()
    try:
        s.add(task)
        s.commit()
        s.refresh(task)
        return task
    finally:
        s.close()
```

**Правило:** Вся логика работы с БД в одном месте!

**CRUD операции:**
- **C**reate — `save_task()`
- **R**ead — `get_tasks_today()`, `get_all_tasks()`
- **U**pdate — `upsert_user_settings()`, `mark_done()`
- **D**elete — `delete_task()`

**Почему?**
- БД может измениться (SQLite → PostgreSQL), меняешь только `database.py`
- Если есть ошибка с данными, ищешь здесь
- Обработчики остаются чистыми

**❌ НЕПРАВИЛЬНО:**
```python
# handlers/commands.py
@router.message()
async def new_task(message: Message):
    s = get_session()  # ← БД логика в обработчике
    task = Task(...)
    s.add(task)
    s.commit()
    s.close()
```

**✅ ПРАВИЛЬНО:**
```python
# database.py
def save_task(task: Task):
    s = get_session()
    s.add(task)
    s.commit()
    s.close()

# handlers/commands.py
from database import save_task
save_task(task)  # ← просто вызываем готовую функцию
```

---

### 4️⃣ `keyboards.py` — UI элементы

```python
CATEGORY_MAP = {
    "≤ 5 минут": "short_5",
    "≤ 30 минут": "short_30",
}

def main_keyboard() -> ReplyKeyboardMarkup:
    return ReplyKeyboardMarkup(keyboard=[...])
```

**Правило:** Все кнопки и маппинги в одном месте!

**Почему?**
- Если нужно изменить текст кнопки, ищешь только здесь
- Маппинги всегда рядом
- UI код не смешивается с логикой обработчиков

**❌ НЕПРАВИЛЬНО:**
```python
# handlers/commands.py
@router.message()
async def show_categories(message: Message):
    kb = InlineKeyboardBuilder()
    kb.button(text="≤ 5 минут", callback_data="cat:short_5")  # ← UI в обработчике
    ...
```

**✅ ПРАВИЛЬНО:**
```python
# keyboards.py
CATEGORY_MAP = {"≤ 5 минут": "short_5"}

# handlers/commands.py
from keyboards import category_keyboard
await message.answer("Выбери категорию:", reply_markup=category_keyboard())
```

---

### 5️⃣ `handlers/commands.py` и `handlers/callbacks.py` — Обработчики

```python
# handlers/commands.py
@router.message(F.text == "📅 Сегодня")
async def today(message: Message):
    settings = get_user_settings(message.from_user.id)
    tasks = get_tasks_today(message.from_user.id, today_date)
    for task in tasks:
        await message.answer(task.description, reply_markup=task_inline(task.id))
```

**Правило:** Обработчики — это оркестр, не музыкант!

Обработчик НЕ должен:
- ❌ Писать в БД
- ❌ Хранить конфиги
- ❌ Создавать кнопки

Обработчик ДОЛЖЕН:
- ✅ Получить данные
- ✅ Обработать логику
- ✅ Отправить ответ

**Аналогия:** 
Обработчик — как официант в ресторане:
- Не готовит еду (это БД)
- Не ремонтирует ресторан (это конфиг)
- Берет заказ → передает на кухню → подает блюдо

---

### 6️⃣ `notifications.py` — Отделенная логика

```python
async def notification_loop():
    while True:
        # Проверяем всех пользователей
        # Отправляем уведомления
        await asyncio.sleep(60)
```

**Правило:** Фоновые процессы отдельно от обработчиков!

**Почему?**
- Обработчики отвечают на сообщения пользователей
- Уведомления отправляются по расписанию
- Они не должны мешать друг другу

---

### 7️⃣ `bot.py` — Главный файл

```python
from handlers.commands import router as commands_router
from handlers.callbacks import router as callbacks_router

dp = Dispatcher()
dp.include_router(commands_router)
dp.include_router(callbacks_router)

async def main():
    init_db()
    asyncio.create_task(notification_loop())
    await dp.start_polling(bot)
```

**Назначение:** Сборка всех частей вместе (как дирижер в оркестре)

**Правило:** Здесь ТОЛЬКО импорты и сборка!

---

## 🎯 Ключевые принципы архитектуры

### 1. DRY (Don't Repeat Yourself)
**Не повторяй код!**

❌ Неправильно:
```python
# handlers/commands.py
s = get_session()
tasks = s.query(Task).filter(...).all()
s.close()

# handlers/other.py
s = get_session()
tasks = s.query(Task).filter(...).all()
s.close()
```

✅ Правильно:
```python
# database.py
def get_tasks_today(user_id, day):
    s = get_session()
    tasks = s.query(Task).filter(...).all()
    s.close()
    return tasks

# handlers/commands.py и handlers/other.py
tasks = get_tasks_today(user_id, day)
```

---

### 2. SOLID принципы

#### S — Single Responsibility (Одна ответственность)
Класс/функция должна отвечать за ОДНО.

✅ `database.py` — только работа с БД
✅ `keyboards.py` — только UI
✅ `handlers/` — только обработка событий

#### O — Open/Closed (Открыто для расширения, закрыто для изменения)
Легко добавлять новое, не ломая старое.

```python
# Нужна новая фичка? Добавляем новый обработчик:
@router.message(F.text == "📊 Статистика")
async def statistics(message: Message):
    # новая фичка, не трогаем старый код
    ...
```

#### I — Interface Segregation (Разделение интерфейсов)
Функции должны принимать только то, что им нужно.

✅ Правильно:
```python
def get_tasks_today(user_id: int, day: date) -> List[Task]:
    # Берет только то, что нужно
```

❌ Неправильно:
```python
def get_tasks_today(user: User):  # ← вся сущность, хотя нужен только ID
```

#### D — Dependency Inversion (Инверсия зависимостей)
Зависим от абстракций, не от конкретных реализаций.

```python
# Легко заменить SQLite на PostgreSQL
# Потому что обработчики зависят от функций в database.py,
# не от конкретной реализации БД
```

---

## 📝 Как писать новую фичу?

### Пример: Добавляем рейтинг задач (0-5 звезд)

**Шаг 1: Модель** (`models.py`)
```python
class Task(Base):
    # существующие поля...
    rating = Column(Integer, nullable=True)  # ← добавляем поле
```

**Шаг 2: БД функции** (`database.py`)
```python
def set_task_rating(task_id: int, user_id: int, rating: int) -> bool:
    """Установить рейтинг задачи"""
    s = get_session()
    try:
        task = s.query(Task).filter_by(id=task_id, user_id=user_id).first()
        if not task:
            return False
        task.rating = rating
        s.commit()
        return True
    finally:
        s.close()
```

**Шаг 3: Кнопки** (`keyboards.py`)
```python
def rating_keyboard(task_id: int):
    kb = InlineKeyboardBuilder()
    for i in range(1, 6):
        kb.button(text="⭐" * i, callback_data=f"rate:{task_id}:{i}")
    kb.adjust(1)
    return kb.as_markup()
```

**Шаг 4: Обработчик** (`handlers/callbacks.py`)
```python
@router.callback_query(F.data.startswith("rate:"))
async def rate_task(callback: CallbackQuery):
    parts = callback.data.split(":")
    task_id = int(parts[1])
    rating = int(parts[2])
    set_task_rating(task_id, callback.from_user.id, rating)
    await callback.message.edit_text(f"✅ Рейтинг: {'⭐' * rating}")
    await callback.answer()
```

**Готово!** Новая фича добавлена, всё на месте.

---

## 🚨 Частые ошибки новичков

### ❌ Ошибка 1: Всё в одном файле
```python
# bot.py (все 500 строк)
import os
from dotenv import load_dotenv
from aiogram import Bot, Dispatcher

BOT_TOKEN = ...  # конфиг
class Task(Base):  # модель
    ...
def get_tasks():  # БД
    ...
def main_keyboard():  # UI
    ...
@dp.message()
async def handler():  # обработчик
    ...
```
**Проблема:** Невозможно разобраться, искать ошибки, добавлять фичи.

---

### ❌ Ошибка 2: Дублирование кода
```python
# handlers/1.py
s = get_session()
task = s.query(Task).filter_by(id=1).first()
s.close()

# handlers/2.py
s = get_session()
task = s.query(Task).filter_by(id=1).first()
s.close()
```
**Проблема:** Если нужно изменить логику получения задачи, меняешь в 10 местах.

---

### ❌ Ошибка 3: БД логика в обработчиках
```python
@router.message()
async def handler(message: Message):
    s = get_session()
    s.add(Task(...))
    s.commit()
    s.close()
```
**Проблема:** Обработчик становится слишком сложным, сложно тестировать.

---

### ❌ Ошибка 4: Жесткие конфиги в коде
```python
# handlers/commands.py
BOT_TOKEN = "12345..."  # ← прямо в коде
API_URL = "https://api.example.com"
```
**Проблема:** Утечка секретов, невозможно использовать в разных окружениях.

---

### ❌ Ошибка 5: Отсутствие обработки ошибок
```python
@router.message()
async def handler(message: Message):
    tasks = get_tasks_today(message.from_user.id, today)  # может быть None
    for task in tasks:  # TypeError: 'NoneType' object is not iterable
        ...
```
**Проблема:** Бот падает молча.

---

## ✅ Лучшие практики

### 1. Типизация (Type Hints)
```python
# ❌ Неясно какой тип
def get_tasks(user_id):
    ...

# ✅ Ясно
def get_tasks(user_id: int) -> List[Task]:
    ...
```

### 2. Docstrings (Документация)
```python
def get_tasks_today(user_id: int, day: date) -> List[Task]:
    """Получить задачи пользователя на конкретный день.
    
    Args:
        user_id: ID пользователя в Telegram
        day: Дата, на которую нужны задачи
    
    Returns:
        Список активных задач
    """
    ...
```

### 3. Обработка ошибок
```python
@router.message()
async def handler(message: Message):
    try:
        tasks = get_tasks_today(message.from_user.id, today)
        if not tasks:
            await message.answer("Задач нет")
            return
        # ...
    except Exception as e:
        logger.error(f"Error: {e}")
        await message.answer("Что-то пошло не так 😞")
```

### 4. Логирование
```python
import logging
logger = logging.getLogger(__name__)

def get_tasks(...):
    logger.info(f"Getting tasks for user {user_id}")
    # ...
```

### 5. .env для секретов
```
BOT_TOKEN=12345...
DATABASE_URL=sqlite:///tasks.db
OPENROUTER_API_KEY=sk-...
```

```python
# Никогда не пишем токены в код!
from dotenv import load_dotenv
load_dotenv()
BOT_TOKEN = os.getenv("BOT_TOKEN")
```

---

## 🧪 Тестирование

Когда код разделен по модулям, тестирование становится простым:

```python
# tests/test_database.py
def test_save_task():
    task = Task(user_id=123, description="Test")
    saved_task = save_task(task)
    assert saved_task.id is not None

def test_get_tasks():
    tasks = get_tasks_today(123, date.today())
    assert isinstance(tasks, list)

# tests/test_keyboards.py
def test_category_keyboard():
    kb = category_keyboard()
    assert kb is not None
```

Без правильной архитектуры тестировать невозможно!

---

## 🔄 Процесс разработки

1. **Планирование**
   - Что нужно добавить?
   - Где это логически должно быть?

2. **Модель** (`models.py`)
   - Какие данные храним?

3. **БД** (`database.py`)
   - Как эти данные хранить, получать, изменять?

4. **UI** (`keyboards.py`)
   - Как пользователь будет взаимодействовать?

5. **Обработчик** (`handlers/`)
   - Как всё это собрать вместе?

---

## 📚 Дополнительные ресурсы

- **aiogram документация** — https://docs.aiogram.dev/
- **SQLAlchemy** — https://docs.sqlalchemy.org/
- **SOLID принципы** — https://habr.com/ru/articles/348286/
- **Clean Architecture** — книга Robert Martin

---

## 💡 Главное правило

> **Код пишется один раз, но читается много раз.**

Пиши код так, чтобы:
- **Ты** смог вернуться через месяц и вспомнить, что здесь происходит
- **Другие разработчики** поняли логику без твоих объяснений
- **Расширение** новыми фичами было просто и понятно

Поздравляем! Теперь ты знаешь как писать правильные ботов! 🚀
